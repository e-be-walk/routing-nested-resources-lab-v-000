<% if flash[:alert] %>
  <div class="alert"><%= flash[:alert] %></div>
<% end %>

<h1><%= @artist.name %></h1>
<ul>
  <% @artist.songs.each do |song| %>
      <li><%= link_to song.title, artist_songs_path(song.artist) %></li>
  <% end %>
</ul>
